<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunqi.common.mapper.ProjectMapper">
    <select id="selectId" resultType="com.yunqi.common.entity.Project">
        # 根据ID查询项目
        select * from project p where id = #{id}
    </select>

    <insert id="insert" parameterType="com.yunqi.common.entity.Project" useGeneratedKeys="true" keyProperty="id">
        # 新增项目
        insert into project (saleId, techID, name, telephone,
                address, type, node, status,
                houseState, situation, village, schedule, region,
                createName, createTime)
            value (
                   #{saleId}, #{techID}, #{name}, #{telephone},
                   #{address}, #{type}, #{node}, #{status},
                   #{houseState}, #{situation}, #{village}, #{schedule}, #{region},
                  #{createName}, now()
            )
    </insert>

    <insert id="update" parameterType="com.yunqi.common.entity.Project">
        # 更新项目
    </insert>
</mapper>