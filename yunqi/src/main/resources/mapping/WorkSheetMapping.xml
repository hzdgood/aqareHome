<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunqi.common.mapper.WorkSheetMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        # 新增
        insert into worksheet (projectId, techIds, dateOfVisit,
                               type, schedule, remark, createName, createTime) value (
                  #{projectId}, #{techIds}, #{dateOfVisit}, #{type},
                  #{schedule}, #{remark}, #{createName}, NOW()
            )
    </insert>

    <insert id="update" parameterType="com.yunqi.common.entity.WorkSheet">
        # 详情
        update worksheet
        set workSummary = #{workSummary},
            visitNode   = #{visitNode},
            updateName  = #{updateName},
            updateTime  = NOW()
        where id = #{id}
    </insert>

    <insert id="updateInfo" parameterType="com.yunqi.common.entity.WorkSheet">
        # 详情
        update worksheet
        set dateOfVisit = #{dateOfVisit},
            techIds     = #{techIds},
            remark      = #{remark},
            updateName  = #{updateName},
            updateTime  = NOW()
        where id = #{id}
    </insert>

    <insert id="complete" parameterType="com.yunqi.common.entity.WorkSheet">
        # 完成
        update worksheet
        set updateName = '吴杰',
            updateTime = NOW()
            where id = #{workId}
    </insert>

    <delete id="delete" parameterType="com.yunqi.common.entity.WorkSheet">
        # 无效
        update worksheet
        set disabled   = 'false',
            updateName = #{updateName},
            updateTime = NOW()
        where id = #{id}
    </delete>
</mapper>