<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunqi.common.mapper.SalesMapper">
    <resultMap id="BaseResultMap" type="com.yunqi.common.entity.Sales">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="telephone" jdbcType="VARCHAR" property="telephone"/>
        <result column="status" jdbcType="VARCHAR" property="status"/>
    </resultMap>

    <select id="select" resultType="com.yunqi.common.entity.Sales">
        select *
        from sales
    </select>

    <select id="selectName" resultType="com.yunqi.common.entity.Sales">
        select *
        from sales where name = #{name}
    </select>

    <insert id="insert" parameterType="com.yunqi.common.entity.Sales" useGeneratedKeys="true" keyProperty="id">
        insert into sales (name, telephone, status) value (#{name},#{telephone},#{status})
    </insert>

    <delete id="delete" parameterType="com.yunqi.common.entity.Sales">
        delete from sales where id = #{id}
    </delete>
</mapper>